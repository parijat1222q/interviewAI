//for future use when we have training data

exports.calculateSkillGaps = (user_sessions) => {
  // MVP: Return empty until you have training data
  return [];
};

exports.predictPerformance = (user_answers) => {
  // MVP: Return baseline until model is trained
  return { predicted_score: 5.0, confidence: 0.3 };
};